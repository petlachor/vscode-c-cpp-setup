{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C/C++: (MinGW) Run",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}\\${fileBasenameNoExtension}.exe",
            "cwd": "${fileDirname}",
            "stopAtEntry": false,
            "externalConsole": false,
            "preLaunchTask": "C/C++: (MinGW) Build"
        }
    ]
}